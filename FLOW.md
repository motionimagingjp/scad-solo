# SCAD-SOLO 画面遷移フロー(最短タップ数)

## ナビ構成: おまかせ / 地図 / ゲーム / マイページ

トップ画面は「今夜の3軒」。シャッフル性は持たせず、近い順に絞った検索結果を
そのまま出す(エンターテイメント性はゲームタブ側で担う。文字の一覧で満足できない人は
地図検索に回ればよいという整理)。ナビのラベルは「おまかせ」(条件を選ばなければ
一番近い3軒が出る、という意味合い)。

AI相談タブは廃止した。モードボタンで大半の要望を1タップで指定できるため、
自由入力(AI検索)が必要なのは「モードにない条件」だけになり、それは「今夜の3軒」画面に
残す方が「ハズレた直後に相談する」動線に合う(タブへの移動という摩擦を挟まない)。
※ 自由入力欄自体はまだ未実装(検索コンテンツ検討中)。

ソロ活タブも廃止し、この枠は「ゲーム」に差し替えた。来店ログはマイページからのリンクで
十分たどり着け、常設タブにするほどではないと判断したため。ゲームタブは企画中のミニゲーム
(一人乾杯タイマー・ソロチン・ソロビンゴ・ドリンクルーレット・強制選曲モード)を一覧表示する
だけの画面で、本体はどれも未実装(`lib/data/activities.ts` の `ALL_GAMES`)。

## メインループ: 起動 → 決定 → 記録 → シェア(実質2タップ)

```
[起動]
  └─ 今夜の3軒(トップ) ……… 0タップ: 基準地(現在地/IP推定)を自動決定し、
       │                        近い順15件からソロ飲み(SOLO_NOMI)の一番近い3軒を即表示
       │                        (乱数抽選ではなく距離順の先頭3件。シャッフル性は持たせない)
       │
       ├─ (任意) モード切替 ……… カウンター席・立ち飲み・せんべろ・日本酒・ワイン・
       │                        ビール・女性ひとり歓迎(複数選択、AND条件)
       │                        「おまかせ」で解除。該当0件なら理由を案内
       │
       ├─ (任意) 「次の3軒」 ……… 距離順プールを3件ずつページ送り。2回まで(無制限だと
       │                        結局「迷う」体験に戻るため)。乱数の引き直しではない
       │
       └─ カードの「今夜はここにする」 ……… ① → /checkin/[spotId]
            └─ 到達と同時にPOST /api/logsが走り記録が確定(確認画面は挟まない。
               決定の意思表示は「今夜はここにする」で完了しているため)
                 └─ 結果画面(1画面に集約)
                      ├─ 決めた店・来店回数・ランク
                      ├─ 実績バナー(新規達成時のみ)
                      ├─ 「Threadsでシェア」 ……… ② → Threads投稿画面へ
                      └─ 今夜のアクティビティ(場所の種類で出し分け・最大3件、乾杯タイマー等はcoming_soon)
```

カードには店名・距離・設備タグに加えて、**推し文**(「一人客が7割」等の一言)と
**住所**、**Googleマップで開くボタン**(座標ベース、同名店舗への誤リンクを防ぐ)を表示する。

結果画面はリロードしても再記録しない(sessionStorageにキャッシュ)。

## 補助ループ: 地図で自分で探す

```
おまかせ/地図タブ → 地図画面
  └─ 場所チップ(現在地 / 駅名指定) + フィルター(複数選択、「今夜の3軒」と同じ7項目)
       └─ ピンをタップ → SpotCardが下からせり上がる
            └─ 「今夜はここにする」→ 上記 ① 以降に合流
```

フィルターの選択肢は「今夜の3軒」(おまかせ)と地図で完全に統一してある(画面ごとに
選択肢が違うと分かりにくいため)。以前はカラオケ・サウナを地図側だけに残していたが、
現在は撤去済み(カラオケ・サウナの検索は現状未対応。データが揃ったら別途検討)。

## 場所の決定順(基準地)

```
手動指定(駅名検索・LocationChip) > 位置情報(GPS) > IP推定(/api/location) > 既定値(渋谷駅)
```

位置情報を拒否したユーザーにもゼロタップで結果を出すため、IP推定を挟む。
「渋谷」のような省略形は自動で「渋谷駅」として検索し、見つからなければ入力のまま再検索する。

## 実績確認ループ

```
ソロ活(/logs。ナビには無く、マイページから遷移): GET /api/me から実データ取得。
           ランク / 未通知の実績バナー(表示と同時に既読化) /
           来店ログ一覧(1件ずつメモの追記・編集と削除ができる)
           メモは VisitLog.comment に保存(使った金額・誰と会ったか等の個人的な記録)
           削除時は残りのログ数を数え直して来店回数とランクを再確定させる(実績は取り消さない)
マイページ: プロフィール要約(ランクは「ソロ活」へのリンク) / アプリ説明 / 関連サービス(来店3件以上で表示)
           来店回数などライブ値を出すため force-dynamic(ビルド時キャッシュ禁止)
```

## 設計上の要点
- ゲーム系機能はナビに置かず、結果画面にだけ出す(検索テンポを守る)
- シェアCTAは結果画面の上段。決めた直後の勢いのまま1タップで投稿できる位置に置く
- スタンプ機能は廃止し、ソロ活タブは来店ログの管理に一本化した(集める体験より、記録を自分で
  整えられること=メモ・削除を優先)
- 「相席なし」「今すぐ入れる」「イベント」フィルターは対応データが無いため削除済み(押しても効かない飾りを作らない方針)
- DB(Neon, シンガポール)を読み書きするAPI/ページは `preferredRegion = "sin1"` を指定し、
  太平洋を挟んだ往復を避ける。POST /api/logsは読み取りを並列化・書き込みを1トランザクションに
  まとめ、往復回数を最小化している
- 店舗データは現状デモ(架空)のみ。実店舗投入は以下の計画で進める(未着手、準備中)

## 実店舗データ収集計画(未着手)

**カバレッジ**: 関東圏、乗降人員上位の駅を対象に、駅ごとに掲載件数のノルマを設定する
(トップ50駅は30店/駅、それ以外〜トップ300駅は9店/駅。最終形で約3,750店舗)。
一気に集めず、まず主要10〜15駅(約200店)でパイプラインの精度・工数を検証してから
段階的に拡大する(Phase 0→1→2)。

**収集フロー**: 駅ごとにAI検索(Gemini+Google検索grounding)で候補店舗(店名・住所・
特徴タグ・出典URL)を発掘 → サンプリングで人力レビュー(上位駅は全件目視、それ以外は
抽出チェック。AI検索のハルシネーション=実在しない店の混入を防ぐため) → 住所を
Geocoding APIで緯度経度に変換 → `scripts/import-spots.ts` でDBへ一括投入
(name+addressの完全一致で重複は更新、新規は作成)。

**閉店確認**: AIに毎回聞き直すのではなく、Places APIの`businessStatus`
(構造化データで信頼性が高い)を月1回のVercel Cron(`vercel.json` → 
`/api/cron/check-closures`)で確認する。閉業が疑われても即削除はせず
`Spot.status`を`NEEDS_REVIEW`にとどめ、人が最終確認してから`CLOSED`にする
(誤検知で実在店を検索結果から消さないため)。`/api/spots`は`status: ACTIVE`の
店だけを返す。

**必要な準備**(未設定): Gemini APIキー(候補発掘用)、サーバー用Google APIキー
`GOOGLE_SERVER_API_KEY`(Geocoding・Places API用。ブラウザ用の
`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`はHTTPリファラー制限がありサーバーから使えない)、
Cron認証用の`CRON_SECRET`。
