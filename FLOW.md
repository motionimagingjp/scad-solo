# SCAD-SOLO 画面遷移フロー(最短タップ数)

## メインループ: 起動 → 決定 → 来店 → シェア(合計 4タップ)

```
[起動]
  └─ さがす(地図) ……… 0タップ: 現在地周辺のピンが即表示。フィルターは前回の選択を保持
       │
       ├─ (任意) フィルター切替 ……… +1タップ/件。トグル式なので打ち直し不要
       │
       └─ ピンをタップ ……… ① → SpotCard が下からせり上がる
            └─ 「この店に決めてチェックイン」 ……… ② → /checkin/[spotId]
                 └─ 到着後「チェックイン」(大ボタン1つ) ……… ③ → POST /api/logs
                      └─ 結果画面(1画面に集約)
                           ├─ スタンプ獲得
                           ├─ 実績バナー(新規達成時のみ)
                           ├─ 「Threadsでシェア」 ……… ④ → Threads投稿画面へ
                           └─ 今夜のアクティビティ(場所の種類で出し分け・最大3件)
                                 🍺 飲み屋   → 乾杯タイマー / ソロチン / ソロビンゴ
                                 🎤 カラオケ → 強制選曲モード
```

## 補助ループ: 迷った時(AI相談)

```
さがす ─「迷ったらAIに相談」(FAB) → AI相談タブ
   └─ モード選択(🍺/🥩/🏃)+ 一言入力 → 送信 → 店舗カード提示
        └─ カード内「チェックイン」→ 上記 ③ 以降に合流
```

## 実績確認ループ

```
ソロ活タブ: ランク / 未通知の実績バナー / スタンプコレクション / 来店ログ
マイページ: プロフィール要約(ランクは「ソロ活」へのリンク) / アプリ説明 / 関連サービス(来店3件以上で表示)
```

## 設計上の要点
- ゲーム系機能はナビに置かず、チェックイン結果画面にだけ出す(検索テンポを守る)
- シェアCTAは結果画面の最上段。スタンプを見た直後の「嬉しい」瞬間に1タップで投稿
- フィルター状態は保持し、2回目以降の起動は「開く→ピン→チェックイン」の3タップで完結
