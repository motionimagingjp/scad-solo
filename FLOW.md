# SCAD-SOLO 画面遷移フロー(最短タップ数)

## ナビ構成: ルーレット / 地図 / ソロ活 / マイページ

AI相談タブは廃止した。モードボタンで大半の要望を1タップで指定できるため、
自由入力(AI検索)が必要なのは「モードにない条件」だけになり、それはルーレット画面に
残す方が「ハズレた直後に相談する」動線に合う(タブへの移動という摩擦を挟まない)。
※ 自由入力欄自体はまだ未実装(検索コンテンツ検討中)。

## メインループ: 起動 → 決定 → 記録 → シェア(実質2タップ)

```
[起動]
  └─ ルーレット(トップ) ……… 0タップ: 基準地(現在地/IP推定)を自動決定し、
       │                        近い順15件からソロ飲み(SOLO_NOMI)を3軒抽選して即表示
       │
       ├─ (任意) モード切替 ……… カウンター席・立ち飲み・せんべろ・日本酒・ワイン・
       │                        ビール・女性ひとり歓迎(複数選択、AND条件)
       │                        「おまかせ」で解除。該当0件なら理由を案内
       │
       ├─ (任意) 「もう3軒」 ……… 2回まで引き直し(無制限だと「迷う」体験に戻るため)
       │
       └─ カードの「今夜はここにする」 ……… ① → /checkin/[spotId]
            └─ 到達と同時にPOST /api/logsが走り記録が確定(確認画面は挟まない。
               決定の意思表示は「今夜はここにする」で完了しているため)
                 └─ 結果画面(1画面に集約)
                      ├─ 決めた店・来店回数・ランク
                      ├─ 実績バナー(新規達成時のみ)
                      ├─ 「Threadsでシェア」 ……… ② → Threads投稿画面へ
                      └─ 今夜のアクティビティ(場所の種類で出し分け・最大3件、乾杯タイマー等はcoming_soon)
```

カードには店名・距離・設備タグに加えて、**推し文**(「一人客が7割」等の一言)と
**住所**、**Googleマップで開くボタン**(座標ベース、同名店舗への誤リンクを防ぐ)を表示する。

結果画面はリロードしても再記録しない(sessionStorageにキャッシュ)。

## 補助ループ: 地図で自分で探す

```
ルーレット/地図タブ → 地図画面
  └─ 場所チップ(現在地 / 駅名指定) + フィルター(複数選択、上記モードに加えカラオケ・サウナも)
       └─ ピンをタップ → SpotCardが下からせり上がる
            └─ 「今夜はここにする」→ 上記 ① 以降に合流
```

ルーレットは SOLO_NOMI(飲み屋)だけを対象にする。カラオケ・サウナ・ソロメシは
地図側でのみ探せる(「ソロ飲み」ルーレットに無関係な種類が混ざらないようにするため)。

## 場所の決定順(基準地)

```
手動指定(駅名検索・LocationChip) > 位置情報(GPS) > IP推定(/api/location) > 既定値(渋谷駅)
```

位置情報を拒否したユーザーにもゼロタップで結果を出すため、IP推定を挟む。
「渋谷」のような省略形は自動で「渋谷駅」として検索し、見つからなければ入力のまま再検索する。

## 実績確認ループ

```
ソロ活タブ: GET /api/me から実データ取得。ランク / 未通知の実績バナー(表示と同時に既読化) /
           来店ログ一覧(1件ずつメモの追記・編集と削除ができる)
           メモは VisitLog.comment に保存(使った金額・誰と会ったか等の個人的な記録)
           削除時は残りのログ数を数え直して来店回数とランクを再確定させる(実績は取り消さない)
マイページ: プロフィール要約(ランクは「ソロ活」へのリンク) / アプリ説明 / 関連サービス(来店3件以上で表示)
           来店回数などライブ値を出すため force-dynamic(ビルド時キャッシュ禁止)
```

## 設計上の要点
- ゲーム系機能はナビに置かず、結果画面にだけ出す(検索テンポを守る)
- シェアCTAは結果画面の上段。決めた直後の勢いのまま1タップで投稿できる位置に置く
- スタンプ機能は廃止し、ソロ活タブは来店ログの管理に一本化した(集める体験より、記録を自分で
  整えられること=メモ・削除を優先)
- 「相席なし」「今すぐ入れる」「イベント」フィルターは対応データが無いため削除済み(押しても効かない飾りを作らない方針)
- DB(Neon, シンガポール)を読み書きするAPI/ページは `preferredRegion = "sin1"` を指定し、
  太平洋を挟んだ往復を避ける。POST /api/logsは読み取りを並列化・書き込みを1トランザクションに
  まとめ、往復回数を最小化している
- 店舗データは現状デモ(架空)のみ。実店舗投入は「ホットペッパーグルメAPI(無料・商用不可)で
  土台を作り、ソロ活タグ(推し文含む)は独自に育てる」方針で検討中。将来的にはAI検索(Gemini+
  Google検索grounding)で見つけた店をDBへ積み上げていく設計も構想中(未着手)
